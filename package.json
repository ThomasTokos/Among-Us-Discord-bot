{
  "name": "among-us-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=development&& npm run ts-node",
    "production": "set NODE_ENV=production&& npm run ts-node",
    "ts-node": "./node_modules/.bin/nodemon --exec \"ts-node --project tsconfig.json\" --files index.ts",
    "build": "./node_modules/.bin/tsc --outDir build --project tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sentry/node": "^5.27.1",
    "@types/node": "^14.14.5",
    "@types/ws": "^7.2.8",
    "discord.js": "^12.3.1",
    "express": "^4.17.1",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0"
  }
}
